{% extends 'base.html.twig' %} {% block title %}{{ activeTasklist.title }}{% endblock %} {% block body %}

<div class="tasklist-page">
    {% include "sidebar.html.twig" %}

    <div class="page-content">
        <div class="form-wrapper">
            <h2>Ajouter une tâche a la liste '{{ activeTasklist.title }}'</h2>
            {{ form_start(taskForm) }}
            {{ form_row(taskForm.title) }}
            {{ form_row(taskForm.priority) }}
            {{ form_row(taskForm.deadline) }}
            {{ form_row(taskForm.status) }}
            <div class="btn-block">
                <button class="btn btn-submit" type="submit" class="btn">Enregistrer</button>
            </div>
            {{ form_end(taskForm) }}
        </div>
    </div>
</div

{% endblock %}
