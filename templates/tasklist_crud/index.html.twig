{% extends 'base.html.twig' %} {% block title %}Créer une liste{% endblock %} {% block body %}

<div class="tasklist-page">
    {% include "sidebar.html.twig" %}

    <div class="page-content">
        <div class="form-wrapper">
            <h2>Créer une nouvelle liste de tâche</h2>
            {{ form_start(tasklistCreationForm) }}
            {{ form_row(tasklistCreationForm.title) }}
            <div class="btn-block">
                <button class="btn btn-submit" type="submit" class="btn">Enregistrer</button>
            </div>
            {{ form_end(tasklistCreationForm) }}
        </div>
    </div>
</div>

{% endblock %}
